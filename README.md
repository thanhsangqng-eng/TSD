
# TS Design CCCD Scanner - GitHub Hosting Guide

Ứng dụng này đã được tối ưu hóa để chạy miễn phí trên **GitHub Pages**. Khi chạy trên GitHub, bạn có thể cài đặt nó vào điện thoại giống hệt như một file APK mà không cần tải file về.

## 🚀 Cách đưa lên GitHub (Miễn phí 100%)

1. **Tạo Repository mới:** Đăng nhập vào GitHub và tạo một repository mới tên là `cccd-scanner`.
2. **Upload Code:** Tải toàn bộ các file này lên repository đó.
3. **Kích hoạt GitHub Pages:**
   - Vào mục **Settings** của repository.
   - Chọn **Pages** ở menu bên trái.
   - Ở phần **Build and deployment**, chọn Branch là `main` (hoặc `master`) và thư mục là `/ (root)`.
   - Nhấn **Save**.
4. **Truy cập:** Đợi khoảng 1-2 phút, GitHub sẽ cung cấp cho bạn một đường link (Ví dụ: `https://ten-cua-ban.github.io/cccd-scanner/`).

## 📲 Cách "Cài đặt" vào điện thoại (Thay thế file APK)

Vì đây là ứng dụng **PWA (Progressive Web App)**, bạn không cần file APK phức tạp:

### Trên Android (Chrome):
1. Mở đường link GitHub Pages của bạn bằng Chrome.
2. Nhấn vào biểu tượng **3 chấm** ở góc trên bên phải.
3. Chọn **"Cài đặt ứng dụng"** (Install App) hoặc **"Thêm vào màn hình chính"**.
4. App sẽ xuất hiện trên màn hình nền với icon và tên riêng, chạy độc lập không có thanh địa chỉ trình duyệt.

### Trên iPhone (Safari):
1. Mở link bằng Safari.
2. Nhấn nút **Share** (hình ô vuông có mũi tên lên).
3. Chọn **"Thêm vào màn hình chính"** (Add to Home Screen).

## 🛠 Ưu điểm so với APK:
- Không bị cảnh báo "Ứng dụng không rõ nguồn gốc".
- Tự động cập nhật khi bạn thay đổi code trên GitHub.
- Dung lượng cực nhẹ, không tốn bộ nhớ máy.
- Kết nối trực tiếp đến MySQL server của bạn (103.77.162.39).
